I will implement the Plan schema and a runtime seeder as requested.

### 1. Database Schema (`prisma/schema/plan.prisma`)
- Create a new schema file for the `Plan` model.
- **Model `Plan`**:
  - `id`: ObjectId
  - `name`: String
  - `price`: Float
  - `currency`: String (default "NGN")
  - `interval`: String (e.g., "Monthly")
  - `features`: String[]
  - `isPopular`: Boolean (default false)
  - `role`: String (default "VENDOR")
  - `createdAt`, `updatedAt`

### 2. Seeding Logic (`src/seeders/planSeeder.ts`)
- Create a new directory `src/seeders`.
- Create `planSeeder.ts` which exports a function `seedPlans`.
- The function will:
  - Check if any plans exist in the database.
  - If count is 0, insert the two plans:
    1.  **Basic**: ₦1500, Monthly, Features: ["Visibility to FicaGo Subscribers", "Post a single service"], Popular: true, Role: VENDOR
    2.  **Premium**: ₦2500, Monthly, Features: ["Visibility to FicaGo Subscribers", "Post multiple services", "Verification badge"], Popular: false, Role: VENDOR
  - Log the result.

### 3. Integration (`src/index.ts`)
- Import `seedPlans` in `src/index.ts`.
- Call `await seedPlans()` immediately after the database connection is established.

### 4. Verification
- Run `npm run prisma-generate`.
- Compile the project to ensure no type errors.
