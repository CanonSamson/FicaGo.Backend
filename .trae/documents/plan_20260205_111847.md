I will implement the Vendor Onboarding feature by defining the database schema, adding validation, and updating the controller logic to handle the form data shown in the screens.

### 1. Database Schema (`prisma/schema/vendor.prisma`)
- Define the `Vendor` model with the following fields to match the frontend screens:
  - `id`: String (ObjectId)
  - `firstName`: String
  - `lastName`: String
  - `email`: String (Unique)
  - `businessType`: String
  - `mobileNumber`: String
  - `serviceCategory`: String
  - `skills`: String[] (Array of strings)
  - `createdAt`: DateTime
  - `updatedAt`: DateTime
- Update `prisma/schema/schema.prisma` to enable `previewFeatures = ["prismaSchemaFolder"]` to support multiple schema files.

### 2. Validation Middleware (`src/middlewares/vendorValidation.ts`)
- Create a Zod schema to validate the incoming request body:
  - `firstName`, `lastName`, `email` (email format), `businessType`, `mobileNumber`, `serviceCategory` as required strings.
  - `skills` as an array of strings.
- Export a validation middleware function using the existing `validateSchema` pattern.

### 3. Controller Implementation (`src/controllers/vendor/onboarding/index.ts`)
- Update `onBoardVendor` to:
  - use `asyncWrapper` and `logger`.
  - Check if a vendor with the same email already exists (return 409 Conflict if so).
  - Create a new `Vendor` record in the database using Prisma.
  - (Optional) Send a welcome email using `sendEmail` service and `welcome-email.html` template.
  - Return a 201 Created response with the vendor data.

### 4. Route Configuration
- **`src/routes/vendor/VendorOnBoardingRoutes.ts`**:
  - Define a POST route `/onboard` that uses the validation middleware and the `onBoardVendor` controller.
- **`src/index.ts`**:
  - Import and register the vendor onboarding routes under `/v1/api/vendor`.

### 5. Verification
- I will run `npm run prisma-generate` to update the Prisma client.
- I will create a test script or use `curl` to verify the endpoint with sample data.
