I will implement the OTP generation and verification process as requested.

### 1. Database Schema (`prisma/schema/otp.prisma`)
- Create a new schema file for the OTP model.
- **Model `Otp`**:
  - `id`: ObjectId
  - `phoneNumber`: String
  - `otp`: String
  - `type`: String (Using String to support "onboarding", "login", "forget", etc. flexibly, but I'll treat it as an enum in the code logic if needed)
  - `expiresAt`: DateTime
  - `createdAt`: DateTime (default now)
  - `updatedAt`: DateTime (updatedAt)
  - Unique constraint on `[phoneNumber, type]` to ensure one active OTP per type per user, or just handle it in logic. I'll stick to a unique constraint or simple index.

### 2. Generate Prisma Client
- Run `npm run prisma-generate`.

### 3. Controller Implementation (`src/controllers/ekyc/phoneNumberVerificationController.ts`)
- Implement `sendOtp` controller:
  - Validate inputs: `phoneNumber` and `type` (required).
  - Use `generateOTP` utility.
  - Set expiry (e.g., 10 minutes).
  - Upsert the OTP record in the database (update if exists for that phone+type, else create).
  - Return the OTP in the JSON response as requested.
- (Optional but recommended) Implement `verifyOtp` controller for the verification step, though the prompt specifically asked for the "generation process". I will focus on the generation part as requested but structure it to allow verification.

### 4. Route Configuration (`src/routes/ekyc/otpRoutes.ts`)
- Create a new route file.
- Define `POST /generate` linked to `sendOtp`.
- Register the new route in `src/index.ts` under `/v1/api/ekyc` or similar.

### 5. Verification
- Use `npm run prisma-generate`.
- Verify compilation with `npx tsc --noEmit`.
